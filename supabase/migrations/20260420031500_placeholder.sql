-- ═══════════════════════════════════════════════════════════
-- HISTORICAL PLACEHOLDER — already applied on remote (Lovable branch).
--
-- The migration file was authored directly in the Supabase Studio /
-- Lovable editor and never committed to the git repo. This placeholder
-- exists only so the Supabase CLI sees matching local + remote history
-- entries. Any logic that may have been introduced here is either
-- superseded or preserved by later explicitly-authored migrations.
-- ═══════════════════════════════════════════════════════════

SELECT 1 WHERE FALSE;
